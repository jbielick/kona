// Generated by CoffeeScript 1.6.2
(function() {
  var Text, regExpEscape;

  exports.Text = Text = (function() {
    function Text(text) {
      this.text = text;
    }

    Text.prototype.regexString = function() {
      return regExpEscape(this.text);
    };

    Text.prototype.test = function(string) {
      return this.text === string;
    };

    Text.prototype.url = function(string) {
      if (this.test(string)) {
        return string;
      } else {
        return false;
      }
    };

    Text.prototype.toString = function() {
      return this.text;
    };

    return Text;

  })();

  regExpEscape = (function() {
    var sRE, specials;

    specials = ['/', '.', '*', '+', '?', '|', '(', ')', '[', ']', '{', '}', '\\'];
    sRE = new RegExp("(\\" + (specials.join('|\\')) + ")", 'g');
    return function(text) {
      return text.replace(sRE, '\\$1');
    };
  })();

}).call(this);
